#!/usr/bin/env bash
# This script stops the 4-to_infinity_and_beyond process.

# Find the PID of the 4-to_infinity_and_beyond process using ps
pid=$(ps -eo pid,comm | grep "4-to_infinity_and_beyond" | grep -v grep | awk '{print $1}')

# If the process exists, kill it
if [ -n "$pid" ]; then
    kill "$pid"
    echo "Process 4-to_infinity_and_beyond (PID $pid) has been stopped."
else
    echo "Process 4-to_infinity_and_beyond not found."
fi
